# ⚙️ Multi-Level RAG System - Configuration File

# =============================================================================
# 📚 PROJECT INFO
# =============================================================================
project:
  name: "Multi-Level RAG System"
  version: "1.0.0"
  description: "نظام RAG متعدد المستويات للمحتوى الديني"

# =============================================================================
# 📂 PATHS
# =============================================================================
paths:
  # المجلدات الرئيسية
  root: "."
  data: "./data"
  build: "./build"
  
  # البيانات المعالجة
  processed_data: "./data/processed"
  documents: "./data/processed/documents.json"
  sections: "./data/processed/sections.json"
  paragraphs: "./data/processed/paragraphs.json"
  
  # قاعدة البيانات
  database: "./data/database"
  chroma_db: "./data/database/chroma_db"
  
  # السجلات
  logs: "./logs"
  cache: "./cache"

# =============================================================================
# 🤖 AI CHUNKING (STEP 2)
# =============================================================================
chunking:
  # Claude API settings
  model: "claude-sonnet-4-20250514"  # أحدث نموذج
  max_tokens: 16000  # أقصى طول للرد
  temperature: 0  # دقة قصوى (بدون إبداع)
  api_key_env: "ANTHROPIC_API_KEY"  # اسم المتغير في .env

  # Prompts location
  prompts_dir: "./build/prompts"

  # Output settings
  save_intermediate: true  # حفظ النتائج المؤقتة
  validate_output: true  # التحقق من البيانات

# =============================================================================
# 🔢 EMBEDDINGS MODEL (STEP 3)
# =============================================================================
embeddings:
  # النموذج (أقوى نموذج للعربية - Microsoft E5)
  provider: "sentence_transformers"
  model: "intfloat/multilingual-e5-large"
  
  # المواصفات
  dimension: 1024  # أكبر من paraphrase (768)
  max_length: 512  # tokens
  normalize: true
  
  # الأداء
  batch_size: 32
  device: "cpu"  # أو "cuda" إذا كان GPU متاح
  show_progress: true
  
  # التخزين المؤقت
  cache_folder: "./cache/embeddings"
