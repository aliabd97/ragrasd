# ⚙️ Multi-Level RAG System - Configuration File

# =============================================================================
# 📚 PROJECT INFO
# =============================================================================
project:
  name: "Multi-Level RAG System"
  version: "1.0.0"
  description: "نظام RAG متعدد المستويات للمحتوى الديني"

# =============================================================================
# 📂 PATHS
# =============================================================================
paths:
  # المجلدات الرئيسية
  root: "."
  data: "./data"
  build: "./build"
  
  # البيانات المعالجة
  processed_data: "./data/processed"
  documents: "./data/processed/documents.json"
  sections: "./data/processed/sections.json"
  paragraphs: "./data/processed/paragraphs.json"
  
  # قاعدة البيانات
  database: "./data/database"
  chroma_db: "./data/database/chroma_db"
  
  # السجلات
  logs: "./logs"
  cache: "./cache"

# =============================================================================
# 🔢 EMBEDDINGS MODEL
# =============================================================================
embeddings:
  # الموفر: "openai" أو "sentence_transformers"
  provider: "openai"  # يمكن تغييره إلى "sentence_transformers"

  # OpenAI Settings
  openai:
    model: "text-embedding-3-small"  # أو "text-embedding-3-large"
    dimension: 1536  # لـ text-embedding-3-small
    # dimension: 3072  # لـ text-embedding-3-large

  # Sentence Transformers Settings (للنموذج المحلي)
  sentence_transformers:
    model: "intfloat/multilingual-e5-large"
    dimension: 1024
    max_length: 512
    normalize: true
    batch_size: 32
    device: "cpu"  # أو "cuda" إذا كان GPU متاح
    show_progress: true
    cache_folder: "./cache/embeddings"

# =============================================================================
# 🤖 LLM MODEL (for Answer Generation)
# =============================================================================
llm:
  provider: "openai"
  model: "gpt-4o-mini"  # أو "gpt-4o" للجودة الأعلى
  temperature: 0.3
  max_tokens: 3000  # لإجابات طويلة ومفصلة
