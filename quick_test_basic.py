"""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ اختبار سريع - نظام RAG القائم على القواعد (بدون LLM)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
هذا الاختبار لا يحتاج API keys - يعمل مباشرة!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"""

import sys
import os

# إضافة المسار للـ build
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'build'))

try:
    from step5_rag_system import RAGSystem
    print("✅ تم استيراد RAGSystem بنجاح\n")
except ImportError as e:
    print(f"❌ خطأ في الاستيراد: {e}")
    print("\n💡 الحل:")
    print("   تأكد أن ملف build/step5_rag_system.py موجود")
    sys.exit(1)

print("="*70)
print("🚀 اختبار نظام RAG - النسخة القائمة على القواعد")
print("="*70)
print()

# أسئلة اختبار
test_queries = [
    "من هو الشريف المرتضى؟",
    "ما هو تعريف الإمامة؟",
]

try:
    # إنشاء نظام RAG
    print("🔄 تهيئة نظام RAG...")
    rag = RAGSystem()

    for i, query in enumerate(test_queries, 1):
        print(f"\n{'━'*70}")
        print(f"سؤال {i}/{len(test_queries)}")
        print(f"{'━'*70}\n")

        response = rag.ask(query)

        if i < len(test_queries):
            input("\n⏸  اضغط Enter للسؤال التالي...")

    print("\n" + "="*70)
    print("✅ الاختبار مكتمل!")
    print("="*70)

    print("\n💡 التالي:")
    print("   - جرب أسئلتك الخاصة")
    print("   - اقرأ AI_POWERED_README.md لإعداد LLM")
    print("   - استكشف example_ai_analyzer.py")

except Exception as e:
    print(f"\n❌ خطأ: {type(e).__name__}")
    print(f"   {str(e)}")
    print("\n💡 تأكد من:")
    print("   1. تشغيل: python build/step3_embeddings_E5.py (إذا لم تشغله بعد)")
    print("   2. تثبيت: pip install -r requirements.txt")

    import traceback
    print("\n🔍 التفاصيل الكاملة:")
    traceback.print_exc()
