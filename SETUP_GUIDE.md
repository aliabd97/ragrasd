# 📥 دليل استيراد المشروع على جهازك

## 🎯 الخطوات الكاملة لبدء العمل على المشروع

---

## الخطوة 1️⃣: Clone المشروع من GitHub

### على Windows (PowerShell أو CMD)

```powershell
# 1. انتقل إلى المجلد الذي تريد وضع المشروع فيه
cd "D:\Projects"  # أو أي مجلد تريده

# 2. Clone المشروع
git clone https://github.com/aliabd97/ragrasd.git

# 3. ادخل إلى مجلد المشروع
cd ragrasd

# 4. انتقل إلى branch التحديثات الجديدة
git checkout claude/fix-source-citations-01EQdDv99XcXKMuospmox71Q
```

---

## الخطوة 2️⃣: إنشاء بيئة افتراضية (Virtual Environment)

### على Windows

```powershell
# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة الافتراضية
.\venv\Scripts\activate

# ستظهر (venv) قبل المسار في terminal
```

### على Mac/Linux

```bash
# إنشاء بيئة افتراضية
python3 -m venv venv

# تفعيل البيئة الافتراضية
source venv/bin/activate
```

---

## الخطوة 3️⃣: تثبيت المكتبات المطلوبة

```powershell
# تأكد من أن البيئة الافتراضية مفعلة (venv)

# ترقية pip أولاً
python -m pip install --upgrade pip

# تثبيت المكتبات
pip install -r requirements.txt
```

**المكتبات التي سيتم تثبيتها:**
- ✅ `openai` - لـ OpenAI API
- ✅ `chromadb` - لقاعدة البيانات المتجهة
- ✅ `sentence-transformers` - للنماذج المحلية
- ✅ `python-dotenv` - لإدارة المتغيرات البيئية
- ✅ `pyyaml` - لملف الإعدادات
- ✅ `tqdm` - لشريط التقدم

---

## الخطوة 4️⃣: إعداد OpenAI API Key

### الحصول على API Key

1. اذهب إلى: https://platform.openai.com/api-keys
2. سجل دخول أو أنشئ حساب
3. اضغط على "Create new secret key"
4. انسخ المفتاح (سيظهر مرة واحدة فقط!)

### إعداد ملف .env

```powershell
# نسخ ملف المثال
copy .env.example .env

# ثم افتح .env في محرر نصوص (مثل Notepad)
notepad .env
```

**محتوى ملف .env:**
```
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

استبدل `sk-proj-xxxx` بالمفتاح الحقيقي الذي حصلت عليه.

---

## الخطوة 5️⃣: التحقق من بنية المشروع

```powershell
# عرض بنية المجلدات
tree /F

# أو استخدم:
dir
```

**يجب أن ترى:**
```
ragrasd/
├── build/
│   ├── step3_embeddings_E5.py
│   ├── step3_embeddings_openai.py      ⭐ جديد
│   ├── step4_query_analyzer.py         ⭐ جديد
│   ├── step5_rag_system.py             ⭐ جديد
│   └── test_embeddings.py
├── data/
│   ├── processed/
│   │   ├── documents.json
│   │   ├── sections.json
│   │   └── paragraphs.json
│   └── database/
│       └── chroma_db/
├── config.yaml                         ⭐ محدث
├── requirements.txt                    ⭐ محدث
├── .env.example                        ⭐ جديد
├── .env                                ⭐ أنشأته أنت
├── interactive_rag.py                  ⭐ جديد
├── USAGE_GUIDE.md                      ⭐ جديد
├── README_NEW.md                       ⭐ جديد
└── GIT_CONFLICT_HELP.md               ⭐ جديد
```

---

## الخطوة 6️⃣: بناء قاعدة البيانات

### الخيار 1: استخدام OpenAI Embeddings (موصى به) ⭐

```powershell
# ادخل إلى مجلد build
cd build

# شغّل السكريبت
python step3_embeddings_openai.py
```

**المدة المتوقعة:** 5-10 دقائق
**التكلفة:** ~$0.10 - $0.20 (منخفضة جداً)

### الخيار 2: استخدام النموذج المحلي (مجاني)

```powershell
cd build
python step3_embeddings_E5.py
```

**المدة المتوقعة:** 15-30 دقيقة
**التكلفة:** مجاني تماماً
**ملاحظة:** يحتاج ذاكرة RAM كافية (4GB+)

---

## الخطوة 7️⃣: تشغيل النظام

### طريقة 1: الواجهة التفاعلية (الأسهل) 🎮

```powershell
# ارجع إلى المجلد الرئيسي
cd ..

# شغّل النظام التفاعلي
python interactive_rag.py
```

**ستظهر لك واجهة تفاعلية:**
```
🚀 نظام RAG التفاعلي للإجابة على الأسئلة الدينية
======================================================================
⚙️ تهيئة نظام RAG...
✅ النظام جاهز!

🔍 سؤالك: ما هو مفهوم الإمامة؟
```

### طريقة 2: من الكود Python

أنشئ ملف `test_my_rag.py`:

```python
from build.step5_rag_system import RAGSystem
from dotenv import load_dotenv

# تحميل .env
load_dotenv()

# تهيئة النظام
rag = RAGSystem(
    embeddings_provider="openai",
    embeddings_model="text-embedding-3-small",
    llm_model="gpt-4o-mini"
)

# طرح سؤال
question = "ما هو مفهوم الإمامة في المذهب الشيعي؟"
result = rag.query(question)

# عرض الإجابة
print("=" * 70)
print("الإجابة:")
print("=" * 70)
print(result['answer'])
print()
print(f"✅ استخدم {result['num_sources']} مصدر")
```

ثم شغّله:
```powershell
python test_my_rag.py
```

---

## الخطوة 8️⃣: التجربة والتخصيص

### تغيير الإعدادات

افتح `config.yaml` وعدّل:

```yaml
# لتغيير نموذج Embeddings
embeddings:
  provider: "openai"  # أو "sentence_transformers"

# لتغيير نموذج LLM
llm:
  model: "gpt-4o"  # للجودة الأعلى (أغلى)
  # أو
  model: "gpt-4o-mini"  # جودة جيدة (أرخص)
```

### تجربة أسئلة مختلفة

```python
أمثلة على الأسئلة:
- "ما هو مفهوم الإمامة؟"
- "من هو الإمام الرضا؟"
- "ما هي صفات الإمام؟"
- "ما الفرق بين النبوة والإمامة؟"
```

---

## 🔧 استكشاف الأخطاء

### خطأ: `OPENAI_API_KEY not found`

**الحل:**
```powershell
# تأكد من وجود ملف .env
dir .env

# افتحه وتأكد من:
# OPENAI_API_KEY=sk-proj-xxxxx
notepad .env
```

### خطأ: `Collection not found`

**الحل:**
```powershell
# شغّل بناء قاعدة البيانات أولاً
cd build
python step3_embeddings_openai.py
```

### خطأ: `No module named 'openai'`

**الحل:**
```powershell
# تأكد من تفعيل البيئة الافتراضية
.\venv\Scripts\activate

# ثم ثبّت المكتبات
pip install -r requirements.txt
```

### خطأ: `MemoryError` (عند استخدام النموذج المحلي)

**الحل:**
- استخدم OpenAI embeddings بدلاً من النموذج المحلي
- أو قلّل `batch_size` في `config.yaml`

---

## 📚 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `interactive_rag.py` | واجهة تفاعلية سهلة |
| `build/step5_rag_system.py` | نظام RAG الكامل |
| `config.yaml` | إعدادات النظام |
| `.env` | OpenAI API Key |
| `USAGE_GUIDE.md` | دليل استخدام شامل |
| `README_NEW.md` | نظرة عامة |

---

## 🎯 الميزات الجديدة

1. ✅ **إجابات طويلة ومفصلة** (5-10 فقرات)
2. ✅ **مصادر واضحة** ("الكافي 1/34" بدلاً من "[المصدر 2]")
3. ✅ **دعم OpenAI** (أسرع وأدق)
4. ✅ **دعم نماذج محلية** (مجاني)
5. ✅ **واجهة تفاعلية** (سهلة الاستخدام)

---

## 💡 نصائح

1. **احفظ API key بأمان** - لا تشاركه مع أحد
2. **استخدم .gitignore** - ملف `.env` لن يتم push لـ GitHub
3. **ابدأ بـ OpenAI** - أسرع وأسهل للتجربة
4. **راجع USAGE_GUIDE.md** - للأمثلة المتقدمة

---

## 🚀 أنت الآن جاهز!

```powershell
# تفعيل البيئة الافتراضية
.\venv\Scripts\activate

# تشغيل النظام
python interactive_rag.py

# استمتع! 🎉
```

---

**إذا واجهت أي مشكلة، أخبرني وسأساعدك! 💪**
